overwrite: true
schema: "https://countries.trevorblades.com/"
documents: "**/*.{gql,graphql}"
generates:
  graphql/generated/schema.ts:
    preset: "client"
    plugins: []
  ./graphql.schema.json:
    plugins:
      - "introspection"
